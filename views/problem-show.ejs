<div ng-include="'/views/navbar.ejs'"> </div>

<div class='container'>
	<h1 class='text-center'>{{problem.title}}</h1>
	<h4 class='text-center'>Time Limit:{{problem.timeLimit}}MS Memory Limit: {{problem.memLimit}}MB</h4>
	<div class='container'>
		<button type='button' class='btn btn-default' ng-click='open()'>Submit</button>
		<button type='button' class='btn btn-default' ng-click='goToStatusList()'>Status</a>
		</div>
		<h2>Description</h2>
		<div class='container'>{{problem.description}}</div>

		<h2>Input</h2>
		<div class='container'>{{problem.input}}</div>

		<h2>Output</h2>
		<div class='container'>{{problem.output}}</div>

		<h2>Sample Input</h2>
		<pre>{{problem.sampleInput}}</pre>

		<h2>Sample Output</h2>
		<pre>{{problem.sampleInput}}</pre>

	</div>

	<script type="text/ng-template" id="submitModal.html">
	<div class="modal-header">
	<h3 class="modal-title">{{problem.title}}</h3>
	</div>
	<div class="modal-body">
	<textarea ng-model='srcCode'></textarea>
	</div>
	<div class="modal-footer">
	<button class="btn btn-default" type="button" ng-click="ok()" ng-disabled='isSubmitting'>Submit</button>
	<button class="btn btn-default" type="button" ng-click="cancel()">Cancel</button>
	</div>
	</script>
